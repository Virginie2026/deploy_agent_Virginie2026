# deploy_agent_Virginie2026

## How to run the script
chmod +x setup_project.sh
./setup_project.sh

## How to trigger the archive
Run the script and press Ctrl+C during execution.
The script will create an archive and delete the incomplete folder.

## Project structure
attendance_tracker_{input}/
├── attendance_checker.py
├── Helpers/
│   ├── assets.csv
│   └── config.json
└── reports/
    └── reports.log

## Requirements
- bash
- python3

